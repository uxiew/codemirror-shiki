import { type ExtraOptions } from "./types/types";

const defaultOptions: ExtraOptions = {
    themeStyle: 'cm',
    defaultColor: 'light',
    cssVariablePrefix: '--shiki-',
}

export default defaultOptions